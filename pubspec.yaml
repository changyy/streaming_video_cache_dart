name: streaming_video_cache
description: >-
  Local caching proxy for streaming video: caches only the byte ranges actually
  played (not whole files), with an LRU size cap. Works with any HTTP player.
version: 1.20260615.1070036
repository: https://github.com/changyy/streaming_video_cache_dart
homepage: https://github.com/changyy/streaming_video_cache_dart
topics:
  - video
  - cache
  - streaming
  - proxy

environment:
  sdk: ^3.11.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
