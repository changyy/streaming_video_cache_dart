name: streaming_video_cache_example
description: Example app for streaming_video_cache (cached video playback).
publish_to: none

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  streaming_video_cache:
    path: ../
  video_player: ^2.9.2
  path_provider: ^2.1.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
